import React from 'react';
import ReactDOM from 'react-dom';

const title = 'My Simple Express React Webpack Babel Setup Environment  7';
ReactDOM.render(
  <div>{title}</div>,
  document.getElementById('app')
);